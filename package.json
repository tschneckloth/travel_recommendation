{
    "name": "travel_recommendation",
    "version": "1.0.0",
    "main": "travel_recommendation.js",
    "devDependencies": {
        "@11ty/eleventy": "^2.0.1",
        "gh-pages": "^6.1.1"
    },
    "scripts": {
        "start": "cross-env ELEVENTY_ENV=development eleventy --serve --incremental",
        "build": "cross-env ELEVENTY_ENV=production eleventy",
        "deploy": "gh-pages -d ./docs",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "cross-env": "^7.0.3"
    }
}
